import React from "react";
import "../Loading/Loading.css";

const Loading = () => {
  return (
    <div className="Loading">
      <h1>loading...</h1>
    </div>
  );
};

export default Loading;
